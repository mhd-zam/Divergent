import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import { ThemeProvider } from "@/components/providers/ThemeProvider";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const jetbrainsMono = JetBrains_Mono({
  variable: "--font-jetbrains-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Divergent — AI-Powered App Builder",
  description:
    "Transform ideas into production-ready applications with AI. Build web apps, mobile platforms, and custom software through simple conversation.",
  keywords:
    "no-code development, AI app builder, software development, web app creator, vibe coding",
  openGraph: {
    title: "Divergent — AI-Powered App Builder",
    description:
      "From idea to app in minutes. AI-powered development platform for creators, entrepreneurs, and developers.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${inter.variable} ${jetbrainsMono.variable} font-sans antialiased`}
      >
        <ThemeProvider>{children}</ThemeProvider>
      </body>
    </html>
  );
}
